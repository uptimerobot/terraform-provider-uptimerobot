---
page_title: "uptimerobot_alert_contacts Data Source - uptimerobot"
subcategory: ""
description: |-
  Lists existing UptimeRobot alert contacts.
---

# uptimerobot_alert_contacts (Data Source)

Lists existing personal UptimeRobot alert contacts with optional filters. Use this to discover alert contact IDs, including mobile app device IDs, before assigning contacts to monitors.

The data source reads public API v3 `GET /user/alert-contacts`. It is read-only and does not manage mobile app notification settings.

The endpoint returns monitor notification contacts such as email, Pro SMS, voice, and mobile app push contacts. Integrations such as Slack, webhook, Telegram, PagerDuty, Discord, and similar channels are exposed through the `uptimerobot_integration` data source instead.

For mobile app push contacts, the public API uses separate type values by platform: `mobile_app_old` for iOS push contacts and `mobile_app` for Android push contacts. The UptimeRobot dashboard groups both under Push notifications.

## Example Usage

```terraform
data "uptimerobot_alert_contacts" "mobile_devices" {
  type   = "mobile_app"
  status = "active"
}

output "mobile_alert_contact_ids" {
  value = data.uptimerobot_alert_contacts.mobile_devices.ids
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `name` (String) Optional exact alert contact name filter.
- `status` (String) Optional alert contact status filter (not_activated, paused, active, to_migrate).
- `type` (String) Optional alert contact type filter (email, pro_sms, mobile_app_old, mobile_app, voice).
- `value` (String, Sensitive) Optional exact alert contact value filter. This may be an email address, phone number, or mobile device value and is stored as sensitive state.

### Read-Only

- `contacts` (Attributes List) Matching personal alert contacts. (see [below for nested schema](#nestedatt--contacts))
- `ids` (List of String) IDs of the matching personal alert contacts.

<a id="nestedatt--contacts"></a>
### Nested Schema for `contacts`

Read-Only:

- `android_push_down_channel` (String) Android push channel used for down notifications on mobile app alert contacts, if returned by the API.
- `android_push_up_channel` (String) Android push channel used for up notifications on mobile app alert contacts, if returned by the API.
- `id` (String) The alert contact ID. Configure this for an exact lookup, or omit it and configure one or more filters.
- `mobile_provider_id` (Number) Mobile provider ID for mobile app alert contacts, if returned by the API.
- `name` (String) The alert contact name.
- `notification_events` (String) The normalized notification event setting: `up_and_down`, `down`, `up`, or `none`.
- `org_alert_contact_id` (Number) Organization alert contact ID, if returned by the API.
- `ssl_expiration_reminder` (Boolean) Whether SSL expiration reminders are enabled for this alert contact.
- `status` (String) The normalized alert contact status.
- `type` (String) The normalized alert contact type.
- `value` (String, Sensitive) The alert contact value, stored as sensitive state.
