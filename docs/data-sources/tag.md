---
page_title: "uptimerobot_tag Data Source - uptimerobot"
subcategory: ""
description: |-
  Looks up one existing UptimeRobot monitor tag.
---

# uptimerobot_tag (Data Source)

Looks up one existing UptimeRobot monitor tag by `id` or exact `name` without managing it.

Tags are created and associated through `uptimerobot_monitor.tags`. A standalone tag resource is intentionally not provided because deleting a tag removes it from monitors and can conflict with monitor ownership.

## Example Usage

```terraform
data "uptimerobot_tag" "production" {
  name = "production"
}

output "production_tag_id" {
  value = data.uptimerobot_tag.production.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) The tag ID. Configure this for an exact lookup, or omit it and configure `name`.
- `name` (String) The exact tag name.
