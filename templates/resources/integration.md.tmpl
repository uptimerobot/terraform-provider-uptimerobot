---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
{{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description | trimspace }}

## Notes

- The UptimeRobot API enforces uniqueness for some integration types by webhook URL, but the check is not fully transactional. Concurrent creates with the same webhook can occasionally yield duplicates or mixed 201/409 responses.
- If you create multiple integrations with the same webhook, prefer serial execution: run `terraform apply -parallelism=1`, or use `depends_on` to force ordering.
- If a create returns "already exists" (409), import the existing integration and re-apply instead of retrying blindly.


## Example Usage

### Slack Integration
{{tffile "examples/resources/uptimerobot_integration/slack.tf"}}

### Webhook Integration
{{tffile "examples/resources/uptimerobot_integration/webhook.tf"}}

### Discord Integration
{{tffile "examples/resources/uptimerobot_integration/discord.tf"}}

### Telegram Integration
{{tffile "examples/resources/uptimerobot_integration/telegram.tf"}}

### Pushover Integration
{{tffile "examples/resources/uptimerobot_integration/pushover.tf"}}

### Pushbullet Integration
{{tffile "examples/resources/uptimerobot_integration/pushbullet.tf"}}

### Multiple Integrations
{{tffile "examples/resources/uptimerobot_integration/multiple.tf"}}

### Google Chat Integration
{{tffile "examples/resources/uptimerobot_integration/google_chat.tf"}}

### Mattermost Integration
{{tffile "examples/resources/uptimerobot_integration/mattermost.tf"}}

### Microsoft Teams Integration
{{tffile "examples/resources/uptimerobot_integration/msteams.tf"}}

### PagerDuty Integration
{{tffile "examples/resources/uptimerobot_integration/pager_duty.tf"}}

### Splunk Integration
{{tffile "examples/resources/uptimerobot_integration/splunk.tf"}}

### Zapier Integration
{{tffile "examples/resources/uptimerobot_integration/zapier.tf"}}

## Integration Types

- `slack` - Slack webhook integration
- `webhook` - Custom webhook integration
- `discord` - Discord webhook integration
- `telegram` - Telegram bot integration
- `pushover` - Pushover notifications
- `pushbullet` - Pushbullet notifications

## Notification Levels

- `1` - All notifications (up, down, paused, etc.)
- `2` - Down notifications only
- `3` - Custom notification settings

{{ .SchemaMarkdown | trimspace }}
